# Build output
bin/
obj/
publish/
*.user

# Local app data / secrets (these never live in the repo, but belt-and-braces)
settings.json
*.log
crash.log

# Release binaries — distributed from the website, not committed
*.exe

# AutoHotkey (GPL-2.0) is bundled into the released build but not redistributed here:
# download AutoHotkey64.exe from autohotkey.com and drop it in to build auto-play.
EchoesUnseen/Resources/Ahk/

# IDE / OS
.vs/
.idea/
*.suo
Thumbs.db
desktop.ini
